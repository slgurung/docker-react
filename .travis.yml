language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t surya/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true surya/docker-react npm run test